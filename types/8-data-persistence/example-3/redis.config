appendonly yes
appendfsync everysec

# Записывать каждую операцию
# appendfsync always

# Записывать данные каждую секунду
# appendfsync everysec — default

# На рассуждение системы (зависит от конфигурации ядра)
# appendfsync no